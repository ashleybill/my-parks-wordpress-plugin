{
	"name": "my-parks/plugin",
	"description": "WordPress plugin for managing park blocks",
	"type": "wordpress-plugin",
	"version": "0.1.0",
	"require-dev": {
		"phpunit/phpunit": "^9.0",
		"yoast/phpunit-polyfills": "^1.0"
	},
	"scripts": {
		"test": "phpunit",
		"setup-tests": "bash bin/install-wp-tests.sh wordpress_test root '' localhost latest"
	}
}
